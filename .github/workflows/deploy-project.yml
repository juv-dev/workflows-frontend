name: Deploy Project

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: |
          echo "🚀 Desplegando versión ${{ inputs.version }}"
          # aquí va AWS / FTP / Vercel / servidor
